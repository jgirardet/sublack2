import subprocess as s

# a = s.Popen(["blackd"])

import requests
import time

# time.sleep(1)


print(requests.post("http://localhost:45484"))

# time.sleep(1)
# time.sleep(10)

# s.call(["taskkill", "/F", "/T", "/PID", str(a.pid)])
